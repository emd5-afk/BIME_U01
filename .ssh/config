Host *
    # Automatically add keys to the running SSH agent so you are not
    # prompted for a passphrase on every new connection.
    AddKeysToAgent yes

    # Keep idle SSH connections alive (important for long VSCode sessions).
    ServerAliveInterval 60
    ServerAliveCountMax 5
